---
interface Props {
	span?: string
	mobileSize?: string
	align?: string
	classes?: string
}

const { span, mobileSize = '12', align, classes } = Astro.props
---

<div
	class:list={[
		'col',
		{ ['col-span-' + `${mobileSize}`]: mobileSize },
		{ ['lg:col-span-' + `${span}`]: span },
		{ ['text-' + `${align}`]: align },
		{ [`${classes}`]: classes }
	]}
>
	<slot />
</div>

<style></style>
