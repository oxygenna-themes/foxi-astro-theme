---
interface Props {
	size?: string
	align?: string
	classes?: string
}

const { size, align, classes } = Astro.props
---

<div
	class:list={[
		'col',
		{ ['col-span-' + `${size}`]: size },
		{ ['text-' + `${align}`]: align },
		{ [`${classes}`]: classes }
	]}
>
	<slot />
</div>
